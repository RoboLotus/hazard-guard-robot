# 순찰 로봇 온도 이상 판단 로직

## 1. 목적과 적용 범위

순찰 로봇이 동일한 설비와 동일한 Voxel을 반복 관측하면서 즉시 고온, 정상 기준선 이탈, 장기 상승 추세를 구분해 판단함.

본 로직은 다음 대상을 기준으로 구성함.

- 폐기물 적치장 표면
- 1차 파쇄기 모터 프레임
- 2차 공정 펌프 베어링 표면
- 압축기 유압탱크 표면
- 선택적으로 연결되는 공기온도 및 유압유 직접 온도센서

본 기준은 시뮬레이션과 현장 시운전을 위한 이상선별 기준이며 제조사 보호계전값, 비상정지 회로, 법정 소방설비를 대체하지 않음.

---

## 2. 순찰 1회와 온도값의 정의

순찰 로봇은 설비별 촬영 지점에서 여러 프레임을 수집하고, 순찰 한 바퀴가 끝날 때 설비 및 Voxel별 대표값 하나를 생성함.

```text
순찰 T0 → 설비별 대표값 저장
순찰 T1 → 설비별 대표값 저장
순찰 T2 → 설비별 대표값 저장
...
순찰 Tn → 설비별 대표값 저장 및 장기 추세 판단
```

여기서 `Tn`은 열화상 카메라의 한 픽셀 온도가 아니라 다음 통계값을 포함하는 순찰 대표 레코드를 의미함.

- `Mean Temperature`: ROI 또는 Voxel 내부 평균온도
- `Median Temperature`: ROI 또는 Voxel 내부 중앙값
- `P90/P95 Temperature`: 상위 온도 분포
- `Max Temperature`: 최고온도
- `Point Count`: 통계 계산에 사용된 유효 Point 수
- `Radiometric Pixel Count`: 실제 열화상 픽셀 수
- `Hot Cluster Pixel Count`: 연속된 고온 픽셀 군집 크기

실시간 즉시 판정에는 설비 ROI의 `P95`와 `Max`를 사용하고, 장기 추세 판정에는 Voxel의 `Median` 이력을 사용함.

한 순찰에서 같은 설비를 여러 프레임 촬영한 경우 프레임별 통계의 시간 중앙값을 해당 순찰의 대표값으로 사용함. 이를 통해 단일 프레임의 반사, 순간 노이즈 또는 일시적 최고점의 영향을 줄임.

---

## 3. 저장 구조

원본 `(x, y, z, Temperature)` Point Cloud는 실시간 분석에 사용함. 모든 Point를 장기간 저장하면 데이터량이 커지고 순찰마다 Point 위치가 달라질 수 있으므로 장기 이력은 Voxel 통계 중심으로 저장함.

```text
순찰 시점 Tn
│
├─ 환경 참고영역
│   └─ Median / Mean / Point Count
│
├─ 같은 재질 기준영역
│   └─ Median / Mean / Point Count
│
└─ 설비 ROI
    ├─ 설비 전체 Mean / Median / P95 / Max
    ├─ Voxel별 Mean / Median / P90 / P95 / Max
    ├─ Voxel별 Point 및 Pixel 수
    └─ 최고온도 발생 Voxel 위치
```

정상 기준선은 순찰 이력과 분리해서 저장함. 이상 상태가 순찰 이력에 포함되더라도 정상 기준선이 같이 올라가며 오염되지 않도록 하기 위함.

정상 기준선 파일에는 다음 값이 필요함.

- `temperature_c`: 동일 운전조건의 정상 대표온도
- `sigma_normal_c`: 정상 운전 중 온도 변동 표준편차
- `sigma_repeat_c`: 동일 조건 반복측정 표준편차
- `sigma_residual_c`: 기준선 모델 잔차의 표준편차
- `sensor_quantization_c`: 카메라 출력의 최소 유효 온도 증분
- `sample_count`: 기준선 계산에 사용된 정상 순찰 수
- `state`: `provisional` 또는 `validated`
- `operating_state`: 기준선을 취득한 운전상태

예비 기준선도 동일 조건의 정상 순찰을 최소 10회 이상 확보해야 사용함.

---

## 4. 공통 데이터 품질 조건

### 4.1 Point와 P95 표본 수

| 항목 | 적용값 | 의미 |
| --- | ---: | --- |
| Voxel 최소 Point | 5 | 점유 및 Median 계산 하한 |
| ROI P95 최소 Point | 40 | P95 판정 운영 하한 |
| ROI P95 권장 Point | 100 | 안정적인 P95 계산 권장값 |
| 최소 고온 픽셀 군집 | 9 | `3×3` radiometric pixel |
| 최소 인접 고온 Voxel | 2 | 공간적으로 연속된 고온 확인 |

ROI의 유효 Point가 40개보다 적으면 해당 ROI의 P95 판정을 보류함. 이때 Median과 Max는 보조 정보로 사용할 수 있지만 표본이 충분한 P95와 동일하게 취급하지 않음.

### 4.2 신뢰도 필터

생산환경의 confidence 임계값은 실제 검증 데이터의 ROC/PR 분석으로 결정해야 함. 시뮬레이션에서는 기능시험용 임시값 `0.50`을 사용함.

### 4.3 실제 현장의 Voxel 크기

시뮬레이션에서는 `0.08m` Voxel을 유지함. 실제 현장에서는 카메라 거리와 IFOV를 이용해 다음 식으로 다시 계산함.

```text
최소 유효 대상 크기 ≈ 3 × 카메라 거리 × IFOV(rad/pixel)
```

실제 현장에서는 IFOV 검증 전까지 `0.08m`를 그대로 안전기준으로 사용하지 않음.

---

## 5. 즉시 온도 판정

설비 ROI의 P95가 유효하고 설비별 Watch, Warning 또는 Critical 기준을 넘는지 확인함.

단, 모든 설비에 하나의 공통 `Twarning/Tcritical`을 적용하지 않음. 측정 위치와 물리량이 다르기 때문에 설비별 기준을 사용함.

### 5.1 폐기물 적치장 표면

| 단계 | 절대온도 P95 | 같은 재질 기준영역 대비 편차 | 추가 조건 |
| --- | ---: | ---: | --- |
| Watch | 40℃ | +10℃ | 즉시 재촬영 및 반사 여부 확인 |
| Warning | 50℃ | +20℃ | 분할·격리·냉각 및 내부 프로브 확인 |
| Critical | 65℃ | - | 또는 동일 조건의 직전 순찰 대비 +20℃ |

폐기물 ΔT는 바닥 표면이 아니라 같은 폐기물 표면의 비고온 기준영역 Median과 비교함.

### 5.2 1차 파쇄기 모터 프레임

생산환경에서는 고정 절대온도를 주 기준으로 사용하지 않고 동일 부하, 동일 냉각상태의 정상 전부하 기준선 `Tbase`를 사용함.

| 단계 | 생산환경 기준 | 판단 조건 |
| --- | ---: | --- |
| Watch | `Tbase+10℃` | 1회 확인 |
| Warning | `Tbase+15℃` | 2회 지속 또는 정상 변동 3σ 초과 |
| Critical 후보 | `Tbase+20℃` | 제조사 alarm/trip, 전류·진동 이상, 급상승 등 추가 근거 필요 |

모터의 `Tbase+20℃`만으로 자동 Critical을 선언하지 않음. 다른 위험신호나 제조사 한계가 확인되지 않으면 Warning 상태로 유지하며 추가 확인함.

실제 기준선이 없는 시뮬레이션에서는 UI와 이벤트 기능시험을 위해 다음 임시 절대값을 사용할 수 있음.

```text
Watch 70℃ / Warning 80℃ / Critical 90℃
```

위 절대값은 시뮬레이션용 임시값이며 생산환경 보호정정값으로 사용하지 않음.

### 5.3 2차 공정 펌프 베어링 표면

| 단계 | 절대온도 P95 | 정상 기준선 편차 | 실제 공기온도 대비 편차 |
| --- | ---: | ---: | ---: |
| Watch | 60℃ | `Tbase+10℃` | +30℃ |
| Warning | 70℃ | `Tbase+15℃` | +35℃ |
| Critical | 75℃ | `Tbase+20℃` | +40℃ |

절대온도, 정상 기준선 편차, 공기온도 대비 편차 중 가장 높은 단계를 사용함. 공기온도 조건은 별도 공기온도 센서가 연결된 경우에만 활성화함. 바닥 Environment ROI를 공기온도로 사용하지 않음.

### 5.4 압축기 유압탱크 표면

유압탱크 표면온도와 유압유 직접 온도를 분리해서 판단함.

#### 탱크 표면 열화상

| 단계 | 정상 표면 기준선 편차 | 시뮬레이션·선별용 절대값 |
| --- | ---: | ---: |
| Watch | `Tbase+5℃` | 45℃ |
| Warning | `Tbase+10℃` | 55℃ |
| Critical 후보 | `Tbase+15℃` | 60℃ |

탱크 표면값만으로 자동 Critical 또는 보호정지를 수행하지 않음. Critical 후보가 발생하면 직접 유온, 압력, 냉각기, 필터, 유량과 제조사 한계를 확인함.

#### 직접 유온센서

| 단계 | 유압유 직접 온도 |
| --- | ---: |
| Watch | 50℃ |
| Warning | 60℃ |
| Critical | 제조사 최대값, 미확인 시 임시 70℃ |

제조사 최대값이 70℃보다 낮으면 제조사 값을 우선함. 직접 유온센서가 연결되지 않으면 직접 유온 판정 채널은 비활성화함.

---

## 6. Max Critical 후보 확인

Max는 단일 반사, 불량픽셀 또는 순간 노이즈의 영향을 받을 수 있으므로 단독으로 즉시 Critical을 선언하지 않음.

Max가 Critical 기준을 넘으면 다음 중 하나 이상을 확인함.

- 고온영역이 최소 `3×3 radiometric pixel`을 차지함
- 2개 이상의 인접 Voxel이 같은 Critical 기준을 넘음
- 다음 순찰 또는 반복 관측에서도 같은 Voxel의 고온이 지속됨
- 연기·가스·전류·진동·압력 등 다른 위험신호가 동반됨

공간 또는 시간 확인이 되지 않은 단일 Max는 Warning 및 재확인 대상으로 처리함.

---

## 7. 장기 온도 상승 추세

장기 추세는 설비 전체 평균이 아니라 동일 Voxel의 순찰별 Median을 비교함. 특정 부품에서만 발생하는 국소 발열이 설비 전체 평균에 묻히는 것을 방지하기 위함.

### 7.1 추세 판단 범위

```text
이력 창: 최근 8회 순찰
최소 추세 관측: 6회 순찰
평가 증분: 6개 값 사이의 5개 증분
minimum_positive_fraction: 1.0
```

6개 값의 5개 증분이 모두 유효 데드밴드보다 크게 상승해야 연속 상승으로 인정함.

### 7.2 유효 데드밴드

작은 센서 오차와 측정 흔들림을 상승으로 판단하지 않도록 다음 식을 사용함.

```text
deadband = max(3×σrepeat, sensor_quantization)
```

시뮬레이션에서는 `0.5℃`를 추가 하한으로 적용함.

```text
simulation_deadband = max(3×σrepeat, sensor_quantization, 0.5℃)
```

### 7.3 최소 누적 상승

고정된 `3℃`, `5℃` 등을 모든 설비에 공통 적용하지 않고 정상 변동과 반복측정 오차를 사용함.

```text
minimum_rise = max(3×σnormal, 3×σrepeat)
```

### 7.4 시간당 상승률

순찰 간격과 설비 부하, 열시정수 자료가 충분하지 않으므로 고정 `℃/hour` 기준은 기본 비활성화함. 시간정보와 계산된 기울기는 기록할 수 있지만 현재 Critical 또는 Warning의 필수 고정 기준으로 사용하지 않음.

### 7.5 추세 판정 단계

| 조건 | 판단 |
| --- | --- |
| 승인된 기준선 없음 | 통계적 장기 추세 판정 비활성화 |
| 6회 연속 상승만 만족 | Watch |
| 6회 연속 상승 + 누적 최소 상승 만족 + 기준선 잔차 3σ 초과 | Warning |
| 추세만으로 Critical | 금지 |

기준선 잔차 조건은 다음과 같음.

```text
Residual = Tcurrent - Tbase
Residual ≥ 3×σresidual
```

---

## 8. 기준값 비교 대상

모든 설비에 바닥 Environment ROI를 공통 ΔT 기준으로 사용하지 않음.

| 대상 | 비교 기준 |
| --- | --- |
| 폐기물 | 같은 적치물의 비고온 표면 Median 및 내부 프로브 |
| 모터 | 동일 부하·회전수·냉각상태의 정상 프레임 기준선 |
| 펌프 | 동일 운전조건의 정상 기준선 또는 별도 공기온도 센서 |
| 유압탱크 | 동일 부하의 정상 탱크 표면 기준선 및 직접 유온센서 |
| 남쪽·동쪽 바닥 ROI | 계절·실내환경 참고값 |

향후 데이터가 충분히 쌓이면 시간대, 계절, 부하, 냉각상태 등을 반영한 예상 정상온도 모델을 별도로 구축할 수 있음.

```text
Texpected = f(운전상태, 부하, 계절, 공기온도, 냉각상태, ...)
```

현재 구현은 위 예측모델을 자동 학습하지 않으며 승인된 정상 기준선과 통계값을 사용함.

---

## 9. 최종 판정 단계

최종 상태는 `Normal / Watch / Warning / Critical` 네 단계로 구성함.

### 9.1 Critical

다음과 같이 설비별 직접 근거가 충분한 경우 Critical로 판단함.

- 유효한 P95가 폐기물 또는 펌프의 Critical 절대온도를 넘음
- 폐기물이 동일 조건의 직전 순찰보다 20℃ 이상 급상승함
- Critical Max가 공간 또는 반복 관측으로 확인됨
- 유압유 직접센서 또는 제조사 보호한계가 Critical에 도달함
- 모터 Critical 후보에 제조사 alarm/trip 또는 다른 위험신호가 동반됨

### 9.2 Warning

다음 조건 중 하나 이상을 만족하면 Warning으로 판단함.

- 설비별 Warning P95 또는 기준선 편차를 넘음
- 장기 연속 상승과 기준선 잔차 3σ 초과가 함께 발생함
- 단독 Critical 후보가 발생했지만 필요한 추가 확인이 아직 없음
- 유압탱크 표면의 Critical 후보가 발생했지만 직접 유온 확인이 없음

### 9.3 Watch

다음 조건은 재확인이 필요한 Watch로 판단함.

- 설비별 Watch 기준을 넘음
- 장기 6회 연속 상승만 확인됨
- 기준선 Warning 후보가 지속성 또는 3σ 조건을 아직 충족하지 않음

### 9.4 Normal

모든 활성 판정 채널이 Watch 이상에 해당하지 않으면 Normal로 판단함.

### 9.5 판정 우선순위

여러 조건이 동시에 발생하면 가장 높은 단계를 최종 상태로 사용함.

```text
Critical > Warning > Watch > Normal
```

---

## 10. 재확인과 우선 재순찰

Watch 또는 추가 확인이 필요한 Warning이 발생하면 다음 절차를 적용하는 것이 적절함.

```text
이상 의심 발생
  ↓
동일 위치 재촬영
  ↓
반사·가림·초점·운전상태 확인
  ↓
필요 시 해당 위치 우선 재순찰
  ↓
P95 / Max / 기준선 편차 / 추세 재평가
```

현재 분석 코드는 Watch, Warning, Critical 이벤트를 생성하지만 `10분 후 자동 재순찰`과 같은 경로 스케줄링은 아직 연결하지 않음. 자동 우선 재순찰은 로봇 경로 제어와 별도로 연동해야 하는 향후 기능임.

---

## 11. 시뮬레이션과 실제 로봇의 구분

### 현재 시뮬레이션

- Voxel 크기 `0.08m` 사용
- confidence 임시 하한 `0.50` 사용
- 정상 기준선이 없으면 σ 기반 장기 추세 판정 비활성화
- 모터는 `70/80/90℃` 시뮬레이션 fallback 사용 가능
- 탱크 표면은 `45/55/60℃` 선별값 사용
- 직접 공기온도와 유온센서가 없으면 해당 판정 채널 비활성화

### 실제 로봇 적용 전 필요한 작업

- 카메라 모델·렌즈·IFOV·정확도·반복성 확인
- 재질별 방사율과 반사 겉보기온도 보정
- 동일 촬영거리·각도·초점 확보
- 설비 운전상태별 정상 기준선 최소 10회 수집
- 제조사 alarm/trip 및 허용온도 확인
- 공기온도 및 직접 유온센서 연결 여부 결정
- confidence 임계값 ROC/PR 검증
- Voxel 크기 재산정

---

## 12. 현재 구현에서 사용하는 주요 설정값

| 설정값 | 현재 적용값 또는 방식 |
| --- | --- |
| `history_window_visits` | 8회 |
| `min_trend_visits` | 6회 |
| `minimum_positive_fraction` | 1.0 |
| `min_points_per_voxel` | 5 |
| `min_points_per_roi_for_p95` | 40 |
| `recommended_points_per_roi_for_p95` | 100 |
| `min_hot_cluster_pixels` | 9 |
| `min_adjacent_hot_voxels` | 2 |
| `deadband` | `max(3σrepeat, sensor_quantization)` |
| 시뮬레이션 deadband 하한 | 0.5℃ |
| `minimum_rise` | `max(3σnormal, 3σrepeat)` |
| 기준선 잔차 | `3σresidual` |
| 고정 `℃/hour` | 비활성화 |
| 추세 단독 Critical | 금지 |
| 기준선 최소 정상 순찰 수 | 10회 |

기준선, σ, 표본수, 센서 누락 상태를 웹 화면에 별도 표시하는 기능은 현재 범위에 포함하지 않음. 기존 웹 위험 이벤트 형식은 그대로 유지함.

---

## 13. 최종 정리

전체 시스템은 다음 원칙으로 구성함.

1. 충분한 표본으로 계산한 설비별 P95를 Watch·Warning의 주 통계로 사용함.
2. Max는 Critical 후보로 사용하되 공간 군집 또는 반복 관측을 요구함.
3. 바닥온도를 모든 설비의 주변온도로 간주하지 않음.
4. 설비별 정상 기준선과 같은 재질 기준영역, 직접 공정센서를 구분해 사용함.
5. 장기 추세는 최소 6회 연속 유효 상승과 정상 분산을 이용함.
6. 고정 `℃/hour`와 공통 누적 상승값을 사용하지 않음.
7. 추세만으로 Critical을 선언하지 않음.
8. 유압탱크 표면과 유압유 직접 온도를 동일한 물리량으로 취급하지 않음.
9. 시뮬레이션 fallback은 현장 보호정정값으로 사용하지 않음.
10. 실제 제조사 한계, 소방절차와 보호회로가 열화상 판정보다 우선함.
